Тебе нужно определиться с ролью, если ты еще не определился смотри файл /Users/leo/VS/TCSONS/AGENTS.MD.
Локально лежит сайт он запущен через докер.
Удаленно загружен сайт на удаленном сервере доступ к нему есть через https://svh05.g-cloud.by или https://tcsonrw-gomel.by или 195.50.11.21, ftp доступ к файлам.
доступ к базе данных через API https://svh05.g-cloud.by/api/service/.
Для взаимодейсвием с БД используй инструкцию /Users/leo/VS/TCSONS/000.DB site/README.md.
Текущий токен для https://tcsonostrovec.by/api/service/:
```text
<хранится вне репозитория>
```
Кабинет администратора личного кабинета сайта https://svh05.g-cloud.by/auth.php - Логин: "admin_maks"

Панель хостинга https://svh05.g-cloud.by:1500 - Логин: "user5233", Пароль: "<хранится вне репозитория>".
FTP доступ к хостингу 195.50.11.21 порт 21 - Логин: "user5233_siteadmin", Пароль: "<хранится вне репозитория>".
Для взаимодейсвия с файлами сервера по FTP используй скрипты из /Users/leo/VS/TCSONS/001.FTP.
Если тебе нужны новые скрипты FTP перед их написанием и использованием спрашивай у меня разрешение.
база  user5233_tcson_site <пароль хранится вне репозитория>
Для добавления новостей на сайт через API используй инструкцию - /Users/leo/VS/TCSONS/102.Gomel ZhD/docs/news-api.md.
Пользователи
- Логин: "admin_maks", Пароль: "<хранится вне репозитория>".
- Логин: "usernews", Пароль: "<хранится вне репозитория>".
volchkovavs — <хранится вне репозитория>
korzhovaev — <хранится вне репозитория>
korzhovakv — <хранится вне репозитория>
svetyuhanm — <хранится вне репозитория>
daynekois — <хранится вне репозитория>
usovale — <хранится вне репозитория>
kulakovskayaae — <хранится вне репозитория>
yuriskonsult — <хранится вне репозитория>

Логин яндекс ID
tcsonzhd - <хранится вне репозитория>

Роль - Контент менеджер:
Задача 1:
С помощью скрипта скачай недостающие последние новости в папку /Users/leo/VS/TCSONS/102.Gomel ZhD/telegramnews/data/tcsonrw_gomel. Начинай с даты, следующей за последней датой в локальном архиве, и заканчивай текущей датой.
- Если системный скрипт не может открыть `t.me` из-за сетевой блокировки, открой публичную ленту через встроенный браузер, собери текущую и предыдущие страницы ленты в JSON и передай его в `main.py` через `--browser-export`. Фото и видео после этого скачиваются по адресам Telegram CDN.
- При сборе видео учитывай только видео самого сообщения. Маленькие WebM-анимации эмодзи внутри текста не являются видео новости и не должны прикрепляться к записи.
- Для каждого настоящего видео обязательно создай кадр или несколько кадров в каталоге `screenshots/video-*`. Сначала используй видеодекодер, затем `qlmanage` как запасной вариант с ограничением времени. Не допускай зависания `qlmanage` на WebM.
- Если Telegram помечает видео как слишком большое и не отдает исходный файл, сохрани видимое превью видео как изображение публикации. Не создавай текстовую запись без медиа.
- После скачивания проверь у каждой выбранной публикации наличие хотя бы одного локального фото, видео или кадра видео. Публикации без медиа, пустые заставки, ссылки без медиа и явные дубликаты не загружай.
- Проверь у каждой публикации категорию `мероприятие` или `новость` и короткий содержательный заголовок. При ошибке добавь исправление в `TITLE_OVERRIDES` или `KIND_OVERRIDES` файла `telegramnews/main.py`.
- Перед загрузкой определи, каких Telegram post ID еще нет на удаленном сайте. Загружай через API только отсутствующие записи; используй `update.php` с `create_if_missing=1`, чтобы повторный запуск не создавал дубли.
- После загрузки проверь удаленные страницы новостей и мероприятий, наличие фото, видео, кадров и правильность заголовков.

Правило оформления сайта:
- На всех страницах использовать единую типографическую шкалу для однотипных элементов.
- Заголовки основных секций: 28 px на компьютере и 24 px на мобильных устройствах.
- Обычный текст и подзаголовки секций: 16 px.
- Заголовки карточек: 18 px на компьютере и 17 px на мобильных устройствах.
- Не задавать отдельному новому блоку увеличенные размеры шрифта, если это не предусмотрено дизайном всего сайта.
- Кнопки одного назначения должны иметь одинаковые размеры, форму, цвета и поведение при наведении; для основных ссылок-кнопок использовать существующий зелёный стиль с красным состоянием при наведении.
